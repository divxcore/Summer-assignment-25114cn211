n = int(input("Enter a number: "))
temp = n
total = 0

while temp > 0:
    digit = temp % 10      # get last digit
    total = total + digit  # add it to sum
    temp = temp // 10      # remove last digit

print(f"Sum of digits = {total}")