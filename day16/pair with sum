arr = list(map(int, input("Enter elements: ").split()))
t = int(input("Enter target sum: "))

n = len(arr)

for i in range(n):
    for j in range(i + 1, n):
        if arr[i] + arr[j] == t:
            print(arr[i], arr[j])